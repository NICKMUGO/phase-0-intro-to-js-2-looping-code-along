// Code your solutions in this file
function writeCards(stringnames,greeting,message,newArray){
    function convertToStr(Number){
        let message = "";
    
    }
    for (let i = 0; i < stringnames.length; i++) {
        stringnames=["Ada", "Brendan", "Ali"];
        greeting="Thankyou";
        message=convertToStr(`${greeting},${stringnames[i]}`);
        newArray=message;
    }
        return newArray;
}
function countdown(integer){
    let i =0
    while (integer<=10){
        let integer=10;
        integer--
        console.log(integer);
    }
}
